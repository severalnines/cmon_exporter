name: Go

#on:
on: [ workflow_dispatch ]
#  push:
#    branches: [ "main" ]
#  pull_request:
#    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v5

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -v ./...

